server {
    listen 80;
    server_name _;
    
    # This is a placeholder configuration for Django API
    # Replace this with your actual Django configuration
    
    location / {
        return 200 "Django API Server - Placeholder\n";
        add_header Content-Type text/plain;
    }
    
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
